name: MDO_AirNet

channels:
  - dlr-sc
  - conda-forge
  - defaults

dependencies:
  # Python version
  - python=3.8

  # Core requirements
  - numpy>=1.21  # Compatível com Python 3.10
  - matplotlib>=3.5
  - scipy>=1.7
  - scikit-learn>=1.0
  - pandas>=1.3
  - deap>=1.3
  - pulp>=2.6
  - numpy-stl>=2.0
  - numba>=0.55
  - smt>=1.0.0

  # Testing dependencies
  - haversine>=2.5


  # Example for pip-installable packages (unavailable in conda)
  # - pip:
  #   - package_name
